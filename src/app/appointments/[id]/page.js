import ScheduleAppointment from "@/components/pageContainers/appointments/ScheduleAppointment";

const page = () => {
    return (
        <div>
            <ScheduleAppointment />
        </div>
    );
};

export default page;